<link rel="stylesheet" href="{{ asset('css/app.css') }}">
<link rel="stylesheet" href="{{ asset('css/normalize.css') }}">
<header>
    <h1 class="h47"><a href="{{ path('main') }}"></a><img src="{{ asset('img/logo-eni.png') }}" alt="Series!"> </h1>
</header>

<div class="h46">
{% block body %}
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
    <h1 class="titremotdepasereset">Réinitialisez <br>votre mot de passe</h1>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.mail) }}
        <div class="input100">
            <small>
                Entrez votre adresse e-mail.
            </small>
        </div>

        <button class="button2">Réinitialisation du mot de passe</button>
    {{ form_end(requestForm) }}
{% endblock %}
</div>