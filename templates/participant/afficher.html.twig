{% extends 'base.html.twig' %}

{% block title %}MonProfil{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
{#    <h1>Hello {{ app.user }}!</h1>#}
    <div class="tableau">

        <div class="col1">
            <div>{{ participant.id }} <img src="{{ asset('img/logo-eni.png') }}" alt="photo"></div>
        </div>
        <div class="col2">
            <h2>{{ participant.pseudo }}</h2>
            <div>Prénom :</div><div>{{ participant.prenom }}</div>
            <div>Nom :</div><div>{{ participant.nom }}</div>
            <div>Téléphone :</div><div>{{ participant.telephone }}</div>
            <div>Email :</div><div>{{ participant.mail }}</div>
            <div>Campus :</div><div>{{ participant.estRattacheA.nom }}</div>
        </div>
        <div class="col1">

        </div>

    </div>

    <button><a href="{{ path('main') }}">Retour</a></button>
</div>


{% endblock %}
