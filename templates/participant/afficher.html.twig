{% extends 'base.html.twig' %}

{% block title %}MonProfil{% endblock %}

{% block body %}
    {# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>#}

<div>
{#    <h1>Hello {{ app.user }}!</h1>#}
    <h2>{{ participant.pseudo }}</h2>

    <div class="tableau">

        <div class="col1">
            {% if participant.imagesParticipant %}
                <img src="{{ asset('uploads/'~participant.imagesParticipant.name) }}" alt="photo" width="150">
            {% endif %}
        </div>
        <div class="tableau">
            <div class="col3">
                <div>Prénom :</div>
                <div>Nom :</div>
                <div>Téléphone :</div>
                <div>Email :</div>
                <div>Campus :</div>
            </div>
            <div class="col3">
                <div >{{ participant.prenom }}</div>
                <div>{{ participant.nom }}</div>
                <div>{{ participant.telephone }}</div>
                <div>{{ participant.mail }}</div>
                <div>{{ participant.estRattacheA.nom }}</div>
            </div>
        </div>


    </div>


</div>
    <div class="colCenter">
        <button><a href="{{ path('main') }}">Retour</a></button>
    </div>


{% endblock %}
